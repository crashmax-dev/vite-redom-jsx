{
  "name": "@redom/monorepo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter='./packages/*' -r run dev",
    "build": "pnpm --filter='./packages/*' -r run build",
    "dev:playground": "pnpm --filter='./playground' run dev",
    "build:playground": "pnpm --filter='./playground' run build"
  },
  "devDependencies": {
    "@crashmax/tsconfig": "^1.0.2",
    "@types/node": "^18.7.18",
    "tsup": "^6.2.3",
    "typescript": "^4.8.3"
  }
}
